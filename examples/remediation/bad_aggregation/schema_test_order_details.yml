# PROPOSED by CASCADE — requires human review before merge
# Guard for: Inflated totals (duplicated line items)
version: 2
models:
  - name: order_details
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns: [order_id, line_item_id]
          name: cascade_guard_order_details_no_fanout_duplicates
