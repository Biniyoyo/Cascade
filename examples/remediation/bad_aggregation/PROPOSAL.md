# Remediation proposal — Inflated totals (duplicated line items)

> Generated by CASCADE from the recorded investigation (`demo/cached/bad_aggregation.json`).
> **Status: PROPOSAL — requires human review and approval. Nothing is auto-applied.**

**Proposed reviewer/owner:** Data Platform Team (from DataHub ownership)

## What broke (evidence-backed likely cause)

- Affected: `urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.ORDER_ENTRY_DB.analytics.order_details,PROD)`
- Likely root cause asset (from column-level lineage): `urn:li:dataset:(urn:li:dataPlatform:snowflake,b2fd91.order_entry_db.order_entry.order_items,PROD)`
- Native incident: `urn:li:incident:67a9bc89-da03-43c3-84ce-2b2b1dbdc670`
- Guard assertion (DataHub): `urn:li:assertion:cascade-b62eafdf0286`

## Proposed repair steps (for the owner to verify & apply)

1. Verify the upstream load for `order_items`: row counts vs previous run, join-key
   integrity, and the load job's run logs.
2. Apply the dbt schema test in `schema_test_order_details.yml` to the model's
   `.yml` so the failure mode is caught at build time — in addition to the DataHub
   guard assertion CASCADE already registered.
3. Backfill/refresh `order_details` after the upstream fix, then resolve the
   DataHub incident.

## Why this is a proposal, not an auto-fix

Lineage identifies the fault domain; confirming the exact defect needs run logs /
row counts that live outside the catalog. CASCADE therefore contains the incident
(native incident + guard + owner alert) and hands the repair decision to a human.
