# Test-only dependencies (CI installs requirements.txt + this file).
# Tests need no DataHub, no Docker, and no API keys.
pytest>=8.0
httpx>=0.27    # fastapi.testclient transport
httpx2>=2.0    # preferred by starlette>=1.3 testclient (silences deprecation)
